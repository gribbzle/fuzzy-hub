import { CheckboxGroupFilterProps } from "@portal/market/ui/organisms";

const genderFilterMock: CheckboxGroupFilterProps = {
  title: "Filter by Gender",
  items: [
    { id: "female", label: "Female", count: 54 },
    { id: "male", label: "Male", count: 21 },
  ],
};

export default genderFilterMock;
