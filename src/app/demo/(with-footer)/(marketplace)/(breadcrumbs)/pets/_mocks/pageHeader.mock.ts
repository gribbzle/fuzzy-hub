import { PageHeaderProps } from "@portal/market/ui/organisms";

const pageHeaderMock: PageHeaderProps = {
  title: "Find a Pet",
  breadcrumbs: [{ label: "Home", href: "/demo" }, { label: "Find a Pet" }],
};

export default pageHeaderMock;
