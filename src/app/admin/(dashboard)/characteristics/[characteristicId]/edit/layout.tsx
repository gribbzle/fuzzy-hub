import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Edit characteristic",
};

export default function EditCharacteristicLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
